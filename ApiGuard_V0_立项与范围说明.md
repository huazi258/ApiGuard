# ApiGuard V0 立项与范围说明

## 0. 文档目的

本文档整合 ApiGuard V0 立项阶段已经确认的产品结论，用于约束后续架构设计、实现和验收。

本文档只定义：

- ApiGuard V0 解决的问题；
- V0 支持的验证任务及统一边界；
- 用户输入、系统输出和完整使用流程；
- 大模型、确定性程序和用户的职责边界；
- V0 必备能力、明确非目标和外部验收标准。

本文档不定义具体代码结构、数据库表、Agent 编排节点、模型供应商或框架选型。尚未确认且必须由后续实现决定的问题，统一列入“待架构阶段决定的问题”。

---

# 1. 项目定位

## 1.1 ApiGuard V0 解决什么问题

正在开发 FastAPI/OpenAPI 服务的后端开发者，即使已经拥有 OpenAPI 接口规范并掌握部分业务规则，仍然需要人工完成以下工作：

1. 阅读接口规范和业务规则；
2. 判断需要验证哪个接口；
3. 构造测试请求和测试数据；
4. 调用真实 HTTP 服务；
5. 对比预期行为和实际响应；
6. 判断结果是否异常；
7. 整理请求、响应、判断依据和复现步骤。

OpenAPI 能够描述接口路径、参数、请求体、响应状态和数据结构，但通常不能完整表达用户身份、资源状态、业务拒绝条件以及操作前后的状态变化。

ApiGuard V0 解决的是：

> 将少量、明确的业务规则和 OpenAPI 规范转换为范围有限、可确认的验证计划，执行真实 HTTP 请求，依据明确条件对比预期行为与实际结果，并输出可复现、可审查的测试结果和疑似缺陷证据。

V0 不承诺理解整个业务系统，也不承诺自动发现所有未知缺陷。

## 1.2 第一类目标用户

ApiGuard V0 的第一类目标用户是：

> 正在开发 FastAPI/OpenAPI 服务，需要在本地环境或测试环境中自行验证接口业务行为的后端开发者。

典型用户包括独立开发者、小型开发团队中的 Python 后端开发者，以及暂时没有专职测试支持、需要自行完成接口验证的开发者。

这类用户应当能够：

- 启动或访问待测服务；
- 提供 OpenAPI 规范；
- 描述需要验证的明确业务规则；
- 提供必要的测试数据和静态认证信息；
- 判断测试计划是否正确，并接受可能产生的测试副作用。

专业测试平台团队、企业质量管理团队和需要完整测试资产管理的组织不是 V0 的第一目标用户。

## 1.3 真实使用场景

首要场景是：

> 后端开发者完成一个接口功能或修改一条业务逻辑后，在本地或测试环境中，用 ApiGuard 验证一个明确目标，并获得可复现的结果证据。

例如，开发者实现订单取消接口后，希望验证：

> 当订单已经支付时，取消操作应被拒绝，并返回明确的业务错误。

开发者向 ApiGuard 提供 OpenAPI、测试环境、业务规则、订单 ID 和静态认证信息。ApiGuard 形成受限验证计划，经用户确认后调用真实接口，并输出请求、响应、预期与实际差异以及最终结论。

首要场景不包括全量回归、生产监控、复杂跨服务流程或无人值守的自主测试探索。

## 1.4 用户当前如何完成这项工作

在没有 ApiGuard 时，用户通常通过 Swagger UI、Postman、curl、pytest 或临时 Python 脚本完成验证：

1. 手工查找接口；
2. 手工准备参数、请求体和认证信息；
3. 手工执行请求；
4. 手工判断响应是否符合业务规则；
5. 必要时再次查询资源状态；
6. 手工复制请求、响应和日志；
7. 手工编写缺陷描述及复现步骤。

这条流程的问题不只是发送请求麻烦，而是业务规则、测试步骤、判断条件和最终证据之间缺少统一、可追溯的对应关系。

## 1.5 与普通 API 测试脚本相比的核心价值

普通 API 测试脚本主要解决：

> 已经知道测试步骤和断言后，如何重复执行这些步骤。

ApiGuard V0 进一步解决：

> 用户只有 OpenAPI、少量明确业务规则和测试数据时，如何形成受限验证计划，执行真实接口，并获得完整证据链。

其核心增量价值包括：

- 将自然语言业务规则规范化为可观察、可比较的验证条件；
- 建立业务规则、接口操作、请求步骤和判断项之间的对应关系；
- 不止生成代码或报告，而是执行真实 HTTP 请求；
- 保存实际请求、实际响应、预期条件和逐项判断依据；
- 将失败结果组织为可复现、可审查的疑似缺陷证据；
- 在规则不明确或证据不足时明确返回无法判断，而不是强行给出缺陷结论。

ApiGuard V0 的核心价值可以概括为：

> 普通测试脚本执行已经写好的测试；ApiGuard V0 将明确业务规则转换为受限验证任务，执行真实接口，并形成从规则到请求、响应和结论的完整证据链。

---

# 2. V0 产品边界

## 2.1 正式冻结的三类任务

### 2.1.1 单接口 OpenAPI 契约验证

针对一个 OpenAPI 操作执行一次 HTTP 请求，验证实际响应是否符合 OpenAPI 中明确声明的契约。

V0 只验证以下内容：

- 响应状态码是否符合声明；
- 响应内容类型是否符合声明；
- JSON 响应结构是否符合声明；
- 必填字段是否存在；
- 字段基础类型是否符合声明。

系统不根据字段名称猜测业务语义，也不把 OpenAPI 中未声明的业务行为视为契约要求。

### 2.1.2 单接口业务规则验证

针对一条明确业务规则，对一个 OpenAPI 操作执行一次 HTTP 请求，并比较实际响应与用户确认的业务预期。

可验证内容包括：

- 应成功或应被拒绝；
- 应返回的状态码；
- 应返回的业务错误码；
- 指定字段应等于某个明确值；
- 指定字段之间应满足明确、可计算的关系；
- 指定字段应出现或不应出现。

该任务只有一次 HTTP 请求，因此不能仅凭这一类任务证明数据库或资源的最终状态变化。

### 2.1.3 最多三步的有限状态流程验证

通过最多三次顺序 HTTP 请求，验证一个业务操作前后的可观察状态变化。

典型流程是：

1. 查询操作前状态；
2. 执行主要业务操作；
3. 查询操作后状态。

统一限制为：

- 只验证一个明确目标；
- 只围绕一个主要资源或一项明确状态关系；
- 最多三个顺序请求；
- 后续请求只能使用用户提供的数据，或前一步响应中明确获得的数据；
- 不允许循环、条件分支、并发或无限重试；
- 不允许自动扩展到其他独立服务。

## 2.2 统一范围限制

所有任务统一遵守以下限制：

- 一个任务只验证一个明确目标；
- 一个任务最多执行三个 HTTP 请求；
- 只根据明确业务规则、OpenAPI 规范和真实响应得出结论；
- 正向、异常和边界测试只是任务内部的测试数据形式，不属于独立任务类型；
- 系统不得在用户未确认的情况下扩大接口范围；
- 无法转化为明确、可观察判断条件的规则不得用于判定缺陷；
- 真实 HTTP 执行前必须由用户确认验证计划、测试数据和可能的副作用；
- V0 只在本地环境或测试环境中执行，不以生产环境为目标。

---

# 3. 输入与输出

## 3.1 用户必须提供的输入

### 3.1.1 每次任务必须提供

用户创建验证任务时必须提供：

- **OpenAPI 规范**：可读取的 OpenAPI 文档；
- **测试环境地址**：实际执行请求的服务地址；
- **非生产环境确认**：确认该环境允许执行计划中的操作；
- **任务类型**：三类冻结任务之一；
- **验证目标**：本次任务具体要证明什么；
- **明确规则或契约要求**：用于形成可观察预期；
- **允许的接口范围**：用户直接指定目标操作，或确认系统从 OpenAPI 推荐的操作；
- **测试数据**：路径参数、查询参数、请求体以及需要使用的资源标识；
- **执行确认**：确认最终结构化验证计划后才允许执行。

### 3.1.2 按任务条件提供

当任务需要时，用户还必须提供：

- 静态 Token、API Key 或固定认证请求头；
- 已存在的用户、订单、商品等资源 ID；
- 可以被修改或删除的测试数据；
- 能够证明业务前置条件的信息；
- 预期状态码、业务错误码、字段值或状态变化规则；
- 验证所需的固定数量、金额、身份或其他业务输入。

V0 不在缺少依据时自行发明认证信息、业务状态、资源 ID 或正确业务预期。

## 3.2 系统可以从 OpenAPI 自动获得的内容

系统可以从 OpenAPI 中提取：

- API 路径和 HTTP 方法；
- `operationId`、摘要和接口描述；
- 路径参数、查询参数和请求头参数；
- 参数是否必填；
- 参数基础类型、枚举、格式及规范中声明的范围；
- JSON 请求体结构；
- 声明的响应状态码；
- 响应内容类型和响应结构；
- 必填响应字段；
- OpenAPI 中声明的安全认证方式；
- `servers` 中列出的候选服务地址；
- 文档中声明的示例值和默认值。

候选服务地址、示例值和默认值只能作为建议，不能直接视为真实测试环境或有效测试数据。

OpenAPI 不能被视为以下信息的可靠来源：

- 实际可用的认证凭据；
- 当前存在的资源 ID；
- 测试环境中的真实数据状态；
- 未写入规范的业务规则；
- 正确的业务错误码；
- 操作是否允许产生副作用；
- 外部依赖当前是否正常；
- 示例数据当前是否仍然有效。

## 3.3 系统最终输出的结果和证据

每次任务最终应输出以下内容。

### 3.3.1 任务信息

- 验证目标；
- 任务类型；
- OpenAPI 来源及可识别版本信息；
- 实际测试环境；
- 目标操作；
- 执行时间；
- 使用的测试数据摘要；
- 脱敏后的认证信息摘要。

### 3.3.2 已确认的结构化验证计划

- 规范化后的业务规则或契约要求；
- 前置条件；
- 请求步骤和执行顺序；
- 每一步的输入来源；
- 每一步的预期结果；
- 最终判断条件；
- 可能的副作用；
- 可能导致无法判断的缺失证据。

### 3.3.3 原始执行证据

每个 HTTP 请求至少记录：

- HTTP 方法；
- 实际请求地址；
- 路径参数和查询参数；
- 脱敏后的必要请求头；
- 请求体；
- 请求时间；
- 响应耗时；
- 实际状态码；
- 必要响应头；
- 实际响应体；
- 请求是否成功到达服务；
- 执行过程中出现的原始错误。

Token、密码、Cookie 和其他凭据不得原样出现在报告中。

### 3.3.4 预期与实际对比

每个判断项必须明确给出：

- 判断项；
- 预期结果；
- 实际结果；
- 是否符合；
- 证据来源。

系统不能只输出“测试通过”或“测试失败”。

### 3.3.5 最终状态与证据报告

最终报告必须包含：

- 四态结论之一；
- 结论的固定判定依据；
- 满足或违反的规则、契约或状态关系；
- 最小复现步骤；
- 实际请求与响应；
- 预期与实际差异；
- 相关测试数据和资源标识；
- 再次执行所需的信息；
- 结论的证据边界。

当状态为 `SUSPECTED_DEFECT` 时，系统输出的是疑似缺陷证据，不自动判断根本原因、严重程度、责任人或最终缺陷归属。

## 3.4 必须返回无法判断的情况

`INCONCLUSIVE` 只用于已经执行了部分或全部真实 HTTP 请求、获得了实际响应，但证据仍不足以支持通过或疑似缺陷的任务。

以下情况必须返回 `INCONCLUSIVE`，不能判定为缺陷：

- 业务前置条件无法由已经获得的证据证明；
- 请求已经执行，但测试数据的真实业务状态无法确认；
- 规则要求验证状态变化，但执行后不存在可用的后置查询或其他外部证据；
- 存在无法排除的并发修改或环境干扰，无法将结果归因于本次操作；
- 请求或响应被截断，导致关键判断证据缺失；
- 实际响应存在，但不足以证明验证通过或存在缺陷。

如果业务规则没有明确、可观察的预期，必须由系统自行发明预期，或者目标接口尚未确认，则任务不得进入真实执行。此类情况属于输入或计划未完成；如果任务形成最终记录，应返回 `EXECUTION_FAILED`，而不是 `INCONCLUSIVE`。

如果 OpenAPI 无法读取、目标服务无法访问、认证失败或请求无法有效执行，也应返回 `EXECUTION_FAILED`。

---

# 4. 完整用户流程

ApiGuard V0 的完整用户流程统一为：

> 输入检查  
> → 规则规范化  
> → 生成结构化验证计划  
> → 用户确认  
> → 真实 HTTP 执行  
> → 确定性比较  
> → 生成四态结论  
> → 输出证据报告  
> → 支持重复验证

## 4.1 输入检查

系统检查：

- 必填输入是否存在；
- OpenAPI 是否可读取；
- 目标操作是否存在；
- 任务是否属于三类冻结任务；
- 是否只有一个验证目标；
- 请求数量是否可能超过三个；
- 数据形式是否与接口定义基本兼容；
- 是否包含 V0 明确不支持的能力；
- 用户是否确认使用本地环境或测试环境。

超出范围的任务必须在真实执行前被拒绝，不能为了“尽量完成”而部分执行。

## 4.2 规则规范化

系统将业务语言整理为：

- 前置条件；
- 执行动作；
- 可观察预期；
- 关键判断字段；
- 缺失的必要信息。

无法形成明确判断条件时，系统应要求补充信息并停止进入执行阶段，不得自行发明规则。若任务在信息未补齐的情况下形成最终记录，应判定为 `EXECUTION_FAILED`。

## 4.3 生成结构化验证计划

验证计划必须包含：

- 验证目标；
- 任务类型；
- 请求步骤；
- 每一步的数据来源；
- 每一步的预期；
- 最终判断条件；
- 可能的副作用；
- 哪些证据缺失会阻止结论。

计划不得包含新的独立目标、循环、条件分支、并发或未经确认的接口扩展。

## 4.4 用户确认

用户必须在执行前确认：

- 接口选择；
- 测试数据；
- 前置条件；
- 业务预期；
- 请求顺序；
- 可能的副作用；
- 测试环境。

未经确认的计划不得发送真实 HTTP 请求。

## 4.5 真实 HTTP 执行

系统按照确认后的计划顺序调用真实服务，最多执行三个请求，并完整保存请求、响应和错误证据。

有限状态流程中的后续步骤只能使用用户预先提供的数据，或前一步响应中明确获得的数据。

## 4.6 确定性比较

系统根据已确认的判断条件进行确定性比较：

- OpenAPI 契约比较；
- 状态码、内容类型和字段结构比较；
- 指定字段值和字段关系比较；
- 操作前后状态值比较；
- 执行步骤完整性检查。

模型不得绕过已确认判断条件，自行修改最终比较标准。

## 4.7 生成四态结论

系统依据执行事实和结构化比较结果，生成：

- `PASSED`
- `SUSPECTED_DEFECT`
- `INCONCLUSIVE`
- `EXECUTION_FAILED`

## 4.8 输出证据报告

系统将计划、真实请求响应、逐项判断、最终状态和复现步骤组织为人类可读报告和结构化证据。

模型生成的文字必须引用已有证据，不能增加未被规则、OpenAPI 或请求响应支持的事实。

## 4.9 支持重复验证

用户能够基于已确认的计划和必要测试数据再次执行同一验证任务。

在测试环境状态已按要求重置的前提下，重复执行应保持：

- 请求步骤一致；
- 核心判断条件一致；
- 事实字段一致；
- 最终状态一致。

报告措辞可以不同，但不得与执行事实和结构化结论矛盾。

---

# 5. 职责边界

## 5.1 大模型负责什么

大模型只负责自然语言与结构化验证条件之间的语义转换：

- 理解用户提供的业务规则；
- 将规则拆解为前置条件、动作和可观察预期；
- 从 OpenAPI 描述中推荐可能相关的目标操作；
- 生成符合 V0 限制的候选验证计划；
- 将自然语言规则转换为可由确定性程序比较的判断条件；
- 根据已经保存的结构化差异生成解释、复现步骤和疑似缺陷描述；
- 在信息不足或接口映射有歧义时指出缺口。

大模型不得：

- 自行扩大验证目标；
- 自行决定未确认的业务预期；
- 自行绕过用户确认执行请求；
- 直接用自由文本分析替代确定性比较；
- 在缺少证据时判定缺陷；
- 添加未观察到的根本原因或系统行为。

## 5.2 确定性程序负责什么

确定性程序负责：

- 检查输入完整性和任务范围；
- 解析 OpenAPI；
- 确认操作、参数和请求体定义；
- 检查计划是否只有一个目标且不超过三个请求；
- 构造和发送真实 HTTP 请求；
- 顺序执行有限状态流程；
- 从前一步响应中提取明确数据供后续步骤使用；
- 保存并脱敏请求、响应和错误；
- 执行状态码、Schema、字段值和前后状态比较；
- 按固定规则分配四态结论；
- 保存结构化证据；
- 支持重复运行。

能够由确定性程序完成的工作，不应为了体现 Agent 而交给大模型。

## 5.3 用户负责什么

用户负责：

- 提供正确的 OpenAPI、测试环境和必要测试数据；
- 提供或确认明确业务规则；
- 提供必要的静态认证信息；
- 确认目标接口和验证计划；
- 确认测试副作用可以接受；
- 确保前置业务状态和测试数据说明真实；
- 决定是否执行任务；
- 根据证据最终接受或驳回疑似缺陷。

## 5.4 核心原则

> 大模型负责将业务语言转换为可验证条件；  
> 确定性程序负责执行、比较和保存证据；  
> 用户负责确认计划，并最终接受或驳回疑似缺陷。

---

# 6. 任务最终状态

## 6.1 `PASSED`

只有同时满足以下条件，任务才能判定为 `PASSED`：

- 确认计划中的必要请求全部执行完成；
- 业务前置条件得到证明；
- 所有明确判断条件均符合预期；
- 没有影响结论的关键证据缺失；
- 保存了完整的请求、响应和逐项比较结果。

## 6.2 `SUSPECTED_DEFECT`

只有同时满足以下条件，任务才能判定为 `SUSPECTED_DEFECT`：

- 业务前置条件得到证明；
- 所需请求全部执行完成；
- 实际结果与至少一项明确预期直接冲突；
- 冲突能够由保存的请求、响应或前后状态证据证明；
- 不存在足以推翻归因的环境或并发歧义；
- 能够提供最小复现步骤。

该状态表示“发现疑似缺陷”，不表示已经完成最终缺陷定性。

## 6.3 `INCONCLUSIVE`

当请求已经部分或全部执行，并获得了真实响应，但现有证据不足以证明通过或疑似缺陷时，判定为 `INCONCLUSIVE`。

典型原因包括：

- 前置状态无法证明；
- 后置状态不可观察；
- 测试数据真实状态不明确；
- 存在无法排除的并发干扰；
- 关键响应证据缺失；
- 规则或目标接口存在尚未解决的歧义。

报告必须明确指出缺少哪项证据，以及为什么缺失会阻止结论。

## 6.4 `EXECUTION_FAILED`

当任务没有完成有效验证时，判定为 `EXECUTION_FAILED`。

典型原因包括：

- OpenAPI 无法读取或解析；
- 目标操作不存在；
- 必要输入缺失；
- 服务不可访问；
- DNS、TLS、网关或连接失败；
- 认证失败；
- 请求无法到达目标业务服务；
- 前一步执行失败，导致必要后续步骤无法继续。

报告必须记录失败步骤、原始错误、已完成步骤和再次执行所需修复的信息。

未经用户确认的任务不得进入执行。用户在执行前主动取消的草稿任务不计为一次完成的验证任务，也不纳入四态验证结果统计。

---

# 7. V0 必备能力

V0 只保留完成业务闭环所必需的能力。

## 7.1 任务输入与范围检查

系统必须能够：

- 接收并检查 OpenAPI、环境地址、验证目标、测试数据和必要认证信息；
- 识别三类冻结任务；
- 阻止多个独立目标、超过三个请求和其他超范围任务；
- 阻止未经确认的真实 HTTP 执行。

## 7.2 OpenAPI 信息提取

系统必须能够从 OpenAPI 中提取完成三类任务所需的路径、方法、参数、JSON 请求体和响应契约信息。

## 7.3 规则规范化与结构化计划

系统必须能够：

- 将明确业务规则转换为前置条件、动作和可观察预期；
- 形成最多三步的结构化候选验证计划；
- 明确每一步输入来源和判断条件；
- 在计划执行前展示给用户确认。

## 7.4 真实 HTTP 执行

系统必须支持：

- 路径参数；
- 查询参数；
- 常规请求头；
- JSON 请求体；
- JSON 响应；
- 用户提供的静态 Token 或 API Key；
- 最多三个顺序请求；
- 从前一步 JSON 响应中获得后续步骤所需的明确值。

## 7.5 确定性结果比较

系统必须支持：

- OpenAPI 契约比较；
- 状态码和内容类型比较；
- JSON 结构、必填字段和基础类型比较；
- 指定字段精确值比较；
- 明确字段关系和数值关系比较；
- 前后状态值比较；
- 执行完整性检查。

## 7.6 四态结论

系统必须按本文件规定的固定边界生成：

- `PASSED`
- `SUSPECTED_DEFECT`
- `INCONCLUSIVE`
- `EXECUTION_FAILED`

## 7.7 证据保存与脱敏

系统必须保存：

- 原始验证目标；
- OpenAPI 标识；
- 用户确认后的计划；
- 实际请求；
- 实际响应；
- 原始执行错误；
- 逐项判断结果；
- 最终状态；
- 人类可读报告和复现信息。

静态凭据及其他敏感信息必须脱敏。

## 7.8 重复验证

系统必须支持在测试环境状态重置后，使用相同计划和必要测试数据重复执行，并比较多次运行的事实结果和最终状态。

---

# 8. 明确非目标

以下能力明确不属于 ApiGuard V0。

## 8.1 测试范围

- 自动覆盖整个 OpenAPI 服务；
- 自动遍历所有接口；
- 大量自动生成测试用例；
- 随机测试、模糊测试和属性测试；
- 性能、压力、稳定性和并发测试；
- 通用安全扫描和渗透测试；
- 任意长度接口流程；
- 超过三个请求的业务工作流；
- 循环、条件分支和并行执行；
- 跨多个独立服务的端到端流程；
- 无限制自主探索。

## 8.2 API 与认证范围

- 支持所有 API 协议；
- WebSocket；
- GraphQL；
- SSE 和流式响应；
- 文件上传与下载验证；
- Webhook 回调验证；
- 长时间异步任务轮询；
- 自动登录；
- OAuth 授权跳转；
- 自动刷新 Token；
- 短信、邮件验证码、MFA 和 CAPTCHA；
- 复杂动态签名协议。

## 8.3 数据、环境和修复能力

- 在生产环境执行测试；
- 自动创建完整测试环境；
- 自动生成复杂业务数据；
- 自动清理或回滚所有副作用；
- 直接访问数据库作为主要判断依据；
- 自动修改或修复业务代码；
- 自动提交修复；
- 自动定位根本原因；
- 自动判断缺陷严重等级、责任人或最终归属。

## 8.4 Agent 与平台能力

- 多 Agent；
- 长期记忆；
- 完整 RAG；
- 自动学习历史任务；
- 自动反思并反复重规划；
- 完整 Web 管理后台；
- 用户、组织、多租户和角色权限系统；
- 团队协作、评论和缺陷管理平台；
- 定时任务、线上监控和消息通知；
- CI/CD 集成；
- 长期统计分析和数据看板。

上述能力只有在 V0 完整验收并重新立项后，才可能作为后续版本候选，不因技术展示需要而提前加入。

---

# 9. 外部完成证据

## 9.1 证明一次任务完成所需的证据

对 `PASSED` 和 `SUSPECTED_DEFECT`，必须同时存在：

1. 明确的验证目标；
2. 用户确认后的结构化验证计划；
3. 目标 OpenAPI 操作及契约依据；
4. 计划中所有必要请求的真实执行记录；
5. 完整且已脱敏的实际请求和实际响应；
6. 每个判断项的预期、实际、结果和证据来源；
7. 最终状态及固定判定依据；
8. 可重复执行的测试数据说明和最小复现步骤；
9. 至少一次基于相同计划的重复运行结果。

对 `INCONCLUSIVE`，必须存在：

- 已执行的真实请求和响应；
- 已获得的有效证据；
- 无法判断的具体原因；
- 缺失证据的明确名称；
- 缺失证据为什么阻止结论的说明。

对 `EXECUTION_FAILED`，必须存在：

- 失败发生的具体步骤；
- 已完成的步骤；
- 实际获得的原始错误；
- 未执行后续步骤的原因；
- 再次执行需要修复或补充的内容。

## 9.2 不能作为完成证据的内容

以下内容单独存在时，不能证明任务完成：

- 页面可以打开；
- 创建任务的接口返回成功；
- 数据库中出现一条任务记录；
- 模型生成了测试计划；
- 模型生成了测试代码；
- 模型生成了报告或缺陷描述；
- 日志中写有“执行成功”；
- 单元测试中的 Mock HTTP 请求成功；
- 报告中写有“发现缺陷”；
- 只保存了状态码，没有保存必要请求响应；
- 只保存了请求响应，没有保存明确判断条件；
- 使用预先写死的响应或报告代替真实 HTTP 执行。

## 9.3 外部可验证原则

每项 V0 能力都必须对应可检查的外部结果：

| 能力 | 外部证据 |
|---|---|
| 读取 OpenAPI | 实际提取的目标操作、参数和响应定义 |
| 理解验证目标 | 规范化后的前置条件、动作和预期 |
| 生成验证计划 | 经用户确认的请求步骤和判断条件 |
| 执行真实接口 | 实际 HTTP 请求、响应和错误记录 |
| 验证契约 | OpenAPI 声明与实际响应的逐项差异 |
| 验证业务规则 | 用户规则、预期值和实际值的对比 |
| 验证状态流程 | 操作前、主要操作和操作后的响应证据 |
| 判定疑似缺陷 | 完整冲突证据和最小复现步骤 |
| 判定无法判断 | 缺失证据及其对结论的影响说明 |
| 支持重复运行 | 同一计划的多次真实运行记录 |

如果某项能力只能通过系统声称“已经理解”或“已经分析”来证明，该能力不视为完成。

---

# 10. V0 验收标准

## 10.1 一次验证任务如何验收

一次任务只有在以下条件满足时，才算完成验收：

- 输入和任务范围经过检查；
- 验证目标已经规范化；
- 结构化计划已经由用户确认；
- 所有必要 HTTP 请求均为真实执行；
- 请求、响应和错误证据已保存并脱敏；
- 所有判断项都有明确预期、实际和证据来源；
- 最终状态符合本文件的固定边界；
- 报告没有加入证据之外的事实；
- 具备再次执行所需的测试数据说明和步骤；
- 重复运行能够获得与环境状态相符且不矛盾的核心结果。

对于 `PASSED` 和 `SUSPECTED_DEFECT`，所有必要步骤必须执行完成。

对于 `INCONCLUSIVE` 和 `EXECUTION_FAILED`，必须准确说明结论为何不能继续，以及下一次执行需要补充或修复什么。

## 10.2 三类任务能力如何验收

V0 必须准备一个可以重置状态、包含已知正确行为和已知缺陷的 FastAPI/OpenAPI 参考服务，并至少完成以下八个固定端到端任务：

| 编号 | 验收任务 | 预期状态 |
|---|---|---|
| 1 | 契约验证：正确响应 | `PASSED` |
| 2 | 契约验证：已知契约错误 | `SUSPECTED_DEFECT` |
| 3 | 单接口业务规则：正确行为 | `PASSED` |
| 4 | 单接口业务规则：已知规则错误 | `SUSPECTED_DEFECT` |
| 5 | 有限状态流程：正确状态变化 | `PASSED` |
| 6 | 有限状态流程：已知状态变化错误 | `SUSPECTED_DEFECT` |
| 7 | 已执行但缺少关键证据的场景 | `INCONCLUSIVE` |
| 8 | 服务、认证或必要执行失败场景 | `EXECUTION_FAILED` |

八个任务必须全部经过：

- 真实 OpenAPI 读取；
- 规则规范化；
- 计划生成和用户确认；
- 真实 HTTP 请求；
- 请求响应保存；
- 确定性比较；
- 四态结论；
- 证据报告生成。

不得使用预先写死的结果绕过任何核心环节。

## 10.3 整个 V0 达到什么条件才算完成

整个 V0 只有同时满足以下条件才算完成：

1. 八个固定端到端验收任务全部得到预期状态；
2. 三类任务均至少包含一个 `PASSED` 和一个 `SUSPECTED_DEFECT` 案例；
3. `INCONCLUSIVE` 和 `EXECUTION_FAILED` 均有真实场景验证；
4. 已知正确行为不能被错误标记为 `SUSPECTED_DEFECT`；
5. 环境、网络或认证失败不能被错误标记为业务缺陷；
6. 所有验收任务均保存完整、脱敏的证据；
7. 所有超范围任务均在真实执行前被阻止；
8. 参考服务状态重置后，八个任务各连续执行三次，共二十四次运行，最终状态全部符合预期；
9. 二十四次运行中的请求步骤、核心判断条件和事实字段保持一致，不因模型措辞变化而改变；
10. 一名未阅读实现代码的人，仅根据报告能够复现至少一个 `PASSED` 任务和一个 `SUSPECTED_DEFECT` 任务；
11. 在另一个不同于参考验收服务的 FastAPI/OpenAPI 服务上，三类任务各至少成功完成一次真实验证；
12. 项目负责人能够解释完整用户流程、大模型与确定性程序的职责边界，以及 `INCONCLUSIVE` 与 `EXECUTION_FAILED` 的区别；
13. 自动测试能够辅助证明范围限制、状态判定和脱敏规则有效，但自动测试不得替代真实 HTTP 端到端证据；
14. 三类核心任务没有遗留为“稍后再补”的未完成能力。

## 10.4 允许进入下一版本的条件

只有在第 10.3 节所有条件满足后，才允许进入下一版本。

进入下一版本前还必须满足：

- V0 的外部验收证据已经保存并可以审查；
- 不存在为了扩展新功能而绕过的 V0 核心缺口；
- 新版本重新明确一个主要扩展目标；
- 一次只选择一个主要扩展方向，不同时扩展多个复杂能力；
- 不因项目名称含有 Agent，就默认加入多 Agent、RAG、长期记忆或无限自主探索。

可能的后续方向只能在重新立项后选择，本文件不提前确定下一版本能力。

---

# 11. 待架构阶段决定的问题

以下问题必须由后续架构阶段决定，本文件不提前回答：

1. 用户通过命令行、API 或最小交互界面完成任务创建、计划确认和报告查看；
2. V0 首个实现精确支持的 OpenAPI 版本范围，以及文档文件和远程地址的读取边界；
3. 结构化验证计划、执行证据和最终报告的具体数据表示与持久化方式；
4. 任务证据的保存位置、保存周期和再次加载方式；
5. 大模型供应商、模型调用方式，以及如何约束模型输出为可校验的结构化结果；
6. 判断条件的具体表达格式、支持的比较运算符和响应字段提取方式；
7. HTTP 请求的超时、重试、重定向、响应体大小和 TLS 处理策略；
8. 静态认证信息的注入、运行时保护和脱敏实现方式；
9. FastAPI 参考验收服务的具体业务内容、已知缺陷注入方式和状态重置机制；
10. 人类可读报告与机器可读证据的具体文件格式。

这些问题不得改变本文件已经冻结的产品范围、三类任务、最多三个请求、四态结论和外部验收原则。
