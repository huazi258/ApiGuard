# ApiGuard V0 架构与系统边界基线

## 0. 文档目的

本文档整合 ApiGuard V0 架构阶段已经冻结的结论，作为后续实现、测试、评审和验收的正式架构基线。

本文档的上位约束是《ApiGuard V0 立项与范围基线》。本文档不重新定义产品定位、目标用户、任务类型、四态结论或 V0 范围，而是将已经冻结的产品闭环映射为：

- 系统上下文和外部边界；
- 核心模块及其职责；
- 核心领域数据对象；
- 任务与验证尝试的状态所有权；
- 确定性编排和失败处理；
- 重试、幂等与变量传递规则；
- 不可变证据边界；
- V0 技术架构与部署形态；
- 软件测试与模型评测边界；
- 任何实现都必须遵守的架构不变量。

本文档不定义：

- 具体 Python 文件和目录结构；
- 完整类、函数和字段设计；
- 数据库表结构；
- API 路由；
- 前端页面和组件细节；
- 具体 Prompt 内容；
- 具体模型供应商；
- 超出 V0 的分布式执行、复杂恢复和自主 Agent 能力。

后续实现不得以技术选型、框架能力或作品展示为理由，突破本文档已经冻结的边界。

---

# 1. 架构目标与核心原则

## 1.1 已冻结的产品闭环

ApiGuard V0 的完整闭环为：

> 输入检查  
> → 业务规则规范化  
> → 生成结构化验证计划  
> → 用户确认  
> → 真实 HTTP 执行  
> → 确定性比较  
> → 四态结论  
> → 证据报告  
> → 重复验证

## 1.2 已冻结的职责边界

- 大模型负责将业务语言转换为候选的可验证条件和候选验证计划；
- 确定性程序负责范围检查、计划校验、真实请求执行、结果比较、四态判定和证据保存；
- 用户负责提供必要输入，确认验证计划和副作用，并最终接受或驳回疑似缺陷。

核心原则是：

> 模型只能提出候选语义；用户确认执行意图；确定性程序拥有执行权和判定权。

## 1.3 已冻结的任务边界

V0 只支持：

1. 单接口 OpenAPI 契约验证；
2. 单接口业务规则验证；
3. 最多三步的有限状态流程验证。

统一限制：

- 一个任务只验证一个明确目标；
- 一个验证尝试最多向待测服务发送三个实际 HTTP 请求；
- 请求必须按确认计划顺序执行；
- 不允许循环、条件分支、并发和执行期自主探索；
- 后续请求只能使用用户输入或前序响应中预先声明的确定性提取值；
- 未经用户确认的计划不得执行；
- 不允许模型在执行期间修改计划、增加请求或调整判断标准。

---

# 2. 系统上下文与外部边界

## 2.1 系统职责

ApiGuard V0 的系统职责是：

> 接收用户输入和 OpenAPI，将业务规则转换为受限且可确认的验证计划；在用户确认后执行真实 HTTP 请求；依据确认计划和 OpenAPI 契约进行确定性比较；生成四态结论并保存可复现、可审查的证据。

## 2.2 外部参与者和依赖

### 2.2.1 后端开发者

后端开发者是唯一主动决策的外部参与者，负责：

- 提供 OpenAPI、测试环境、验证目标、测试数据和必要认证信息；
- 确认环境不是生产环境；
- 确认目标接口、前置条件、请求步骤、业务预期和副作用；
- 决定是否创建验证尝试；
- 决定是否重复运行；
- 根据证据最终接受或驳回疑似缺陷。

ApiGuard 不替代用户确认业务真实性和测试副作用。

### 2.2.2 OpenAPI 来源

OpenAPI 来源是外部输入，不是完整业务事实来源。

ApiGuard 可以从中提取：

- 路径和 HTTP 方法；
- 操作标识和描述；
- 参数和请求体定义；
- 响应状态码、内容类型和 Schema；
- 规范声明的认证方式。

ApiGuard 不得把 OpenAPI 中的示例值、默认值或服务器地址直接视为真实有效的测试数据和测试环境。

### 2.2.3 待测 API 服务

待测服务是 ApiGuard 执行真实 HTTP 请求的外部系统。

ApiGuard 只依据 HTTP 层可获得的事实进行判断，不负责：

- 控制待测服务内部实现；
- 直接查询数据库补充证据；
- 自动定位根因；
- 修改或修复业务代码；
- 保证服务和外部依赖可用。

### 2.2.4 大模型服务

大模型服务是受限的语义转换依赖，不是任务控制者、执行者或事实来源。

大模型不得：

- 获取待测 API 调用能力；
- 读取运行时认证凭据；
- 直接创建验证尝试；
- 修改已确认计划；
- 覆盖确定性比较结果；
- 直接生成权威四态结论。

### 2.2.5 证据存储

证据存储用于持久化：

- 任务和计划快照；
- 验证尝试；
- 实际请求和响应；
- 原始异常；
- 比较输入和比较结果；
- 四态结论；
- 报告和重复运行记录。

“保存什么、何时冻结、如何脱敏”属于 ApiGuard 的职责。

### 2.2.6 测试环境状态与重置

测试环境状态恢复由用户或待测环境负责。

ApiGuard 可以记录用户关于状态已重置的声明，也可以保存可观察的前后状态证据，但不承诺自动恢复、回滚或清理所有测试副作用。

---

# 3. 应用整体形态

## 3.1 模块化单体

ApiGuard V0 采用模块化单体。

以下能力运行在同一个应用进程中：

- FastAPI HTTP 服务；
- 最小服务端页面；
- 任务状态机与确定性编排；
- OpenAPI 获取和解析；
- 大模型调用适配器；
- HTTP 执行器；
- 确定性比较器；
- 证据封存与报告生成；
- 数据持久化访问。

大模型实际推理由外部模型服务完成，待测 API 和参考验收服务作为外部进程或容器运行。

## 3.2 单进程边界

V0 采用：

- 一个应用进程；
- 一个 Uvicorn Worker；
- 一个本地 SQLite 数据文件；
- 同一验证任务同一时刻最多一个 `EXECUTING` 验证尝试。

V0 不需要独立 Worker、后台任务系统或消息队列。

## 3.3 不采用微服务和 Kubernetes

V0 不存在以下需求：

- 高并发；
- 多执行节点；
- 多区域部署；
- 独立模块扩缩容；
- 长时间任务调度；
- 分布式状态协调。

拆分微服务会额外引入跨服务调用失败、状态一致性、分布式日志、密钥传递和部署复杂度，不能提高 V0 核心闭环的可信度。

Kubernetes、Helm 和服务网格不属于 V0。

---

# 4. 七个核心模块

## 4.1 任务生命周期与范围控制模块

### 负责

- 创建和管理验证任务；
- 检查任务类型、单一目标和非生产环境确认；
- 控制合法状态转换；
- 阻止超范围任务进入后续阶段；
- 管理用户确认门禁；
- 创建首次执行和重复运行的验证尝试；
- 保证同一任务最多一个活动尝试；
- 将其他模块的稳定输出按顺序传递。

### 不负责

- 不理解业务规则语义；
- 不解析响应 Schema；
- 不构造真实 HTTP 请求；
- 不进行字段比较；
- 不根据报告文字决定状态；
- 不执行自主重规划。

### 实现性质

必须完全确定性。

---

## 4.2 OpenAPI 上下文模块

### 负责

- 获取和解析 OpenAPI；
- 提取与任务相关的操作、参数、请求体和响应契约；
- 形成可冻结的 OpenAPI 上下文快照；
- 为候选计划生成、计划校验、执行和契约比较提供统一依据；
- 记录文档来源、可识别版本和解析诊断。

### 不负责

- 不猜测业务语义；
- 不把示例值当作真实测试数据；
- 不决定验证目标；
- 不执行 HTTP 验证；
- 不生成四态结论。

### 实现性质

必须完全确定性。

---

## 4.3 规则规范化与候选计划模块

### 负责

- 将用户自然语言规则拆解为前置条件、动作和可观察预期；
- 识别缺失信息和歧义；
- 根据相关 OpenAPI 上下文提出候选操作映射；
- 生成符合 V0 表达形式的一至三个候选请求步骤；
- 将业务预期转换为候选结构化判断条件。

### 不负责

- 不判断候选计划是否合法；
- 不发明认证信息、资源 ID 或业务状态；
- 不创建验证尝试；
- 不调用待测 API；
- 不修改已确认计划；
- 不生成权威四态结论。

### 实现性质

这是唯一需要大模型参与的核心模块。其输出始终是不可信候选数据，必须经过本地结构校验和确定性计划校验。

---

## 4.4 计划校验模块

### 负责

- 检查是否只有一个验证目标；
- 检查是否最多三个请求；
- 检查是否仅为顺序执行；
- 阻止循环、分支、并发和执行期探索；
- 校验目标操作是否存在；
- 校验请求输入和变量来源是否明确；
- 校验判断条件是否可由 V0 确定性执行；
- 校验副作用和证据缺失条件是否明确；
- 形成可供用户确认的已校验计划版本。

### 不负责

- 不改写用户业务规则；
- 不通过猜测补全预期；
- 不执行真实请求；
- 不根据响应判断缺陷。

### 实现性质

必须完全确定性。

---

## 4.5 HTTP 执行模块

### 负责

- 只依据已确认计划构造请求；
- 在运行时注入用户提供的静态认证信息；
- 按顺序执行最多三个实际 HTTP 请求；
- 记录每次实际发送和响应事实；
- 按确认计划提取前序响应中的变量；
- 将变量绑定到后续请求；
- 在必要步骤失败后停止后续执行；
- 保存网络错误、超时和用户中止等原始异常。

### 不负责

- 不读取用户自由文本或模型自由文本；
- 不临时增加请求；
- 不选择新接口；
- 不改变请求顺序；
- 不根据响应自由重规划；
- 不判断最终业务结论。

### 实现性质

必须完全确定性。

---

## 4.6 确定性比较与结论模块

### 负责

- 比较实际状态码、内容类型和 OpenAPI 声明；
- 验证 JSON 结构、必填字段和基础类型；
- 比较指定字段精确值；
- 比较明确字段关系和数值关系；
- 比较操作前后状态；
- 检查必要步骤是否完整；
- 判断前置条件是否有证据支持；
- 判断是否存在关键证据缺失；
- 按固定规则生成四态业务结论。

### 不负责

- 不用模型分析替代确定性比较；
- 不修改用户确认的判断条件；
- 不增加新的判断项；
- 不推断根因、严重程度或责任人。

### 实现性质

必须完全确定性。

---

## 4.7 证据、报告与重复运行模块

### 负责

- 保存输入、快照、执行事实和比较结果；
- 在持久化前完成敏感信息脱敏；
- 封存不可修改的证据包；
- 基于证据包生成机器可读和人类可读报告；
- 保存报告版本；
- 基于同一确认计划提出重复运行请求；
- 对多次独立尝试生成派生对比。

### 不负责

- 不改变执行事实；
- 不重新决定四态结论；
- 不补充未观察到的事实；
- 不把重复运行变成重新规划；
- 不覆盖旧证据和旧结论。

### 实现性质

证据保存、脱敏、封存和事实报告必须确定性。报告措辞可以重新生成，但不得改变事实和结论。

---

# 5. 核心领域数据对象

V0 保留八个稳定领域对象和一个派生报告对象。

## 5.1 验证任务 `VerificationTask`

### 代表什么

代表用户希望完成的一个稳定验证目标，是任务准备、计划确认和历次验证尝试的聚合根。

### 由谁产生

任务生命周期与范围控制模块。

### 被谁使用

所有核心模块通过稳定引用使用。

### 必须包含

- 唯一任务标识；
- 三类任务之一；
- 唯一验证目标；
- 用户原始规则或契约要求；
- OpenAPI 来源引用；
- 测试环境；
- 非生产环境确认；
- 用户测试数据；
- 允许的接口范围；
- 当前任务生命周期状态；
- 当前确认计划引用；
- 历次验证尝试引用。

### 暂不决定

- 标识格式；
- 完整数据库字段；
- 搜索、分页和归档；
- 用户、组织和权限体系。

---

## 5.2 OpenAPI 上下文快照 `OpenAPIContextSnapshot`

### 代表什么

代表某个计划形成时实际使用的 OpenAPI 事实依据。

### 由谁产生

OpenAPI 上下文模块。

### 被谁使用

规则规范化、计划校验、HTTP 执行、确定性比较和证据模块。

### 必须包含

- OpenAPI 来源标识；
- 可识别版本或内容摘要；
- 相关操作的路径、方法和操作标识；
- 参数定义；
- JSON 请求体契约；
- 响应状态码、内容类型和响应结构；
- 认证方式；
- 解析异常和缺失信息；
- 快照覆盖的操作范围。

### 暂不决定

- 具体规范化表示；
- 是否保存完整原文和压缩形式；
- 摘要算法；
- `$ref` 等内部解析策略。

---

## 5.3 规范化规则 `NormalizedRule`

### 代表什么

代表模型将用户业务语言转换后的结构化候选语义。

### 由谁产生

规则规范化与候选计划模块。

### 被谁使用

计划校验、用户确认展示、比较结果追溯和报告生成。

### 必须包含

- 原始规则引用；
- 前置条件；
- 执行动作；
- 可观察预期；
- 关键判断字段或状态；
- 已识别歧义；
- 缺失信息；
- 与 OpenAPI 操作的候选映射。

### 暂不决定

- 完整结构化语法；
- 字段路径表达；
- 模型输出技术细节；
- 模型置信度展示。

模型置信度不得作为执行或四态判定依据。

---

## 5.4 验证计划快照 `ValidationPlanSnapshot`

### 代表什么

代表系统准备执行的一份完整验证方案。

一份计划需要区分：

1. 候选计划；
2. 确定性校验通过的计划；
3. 用户确认后的冻结计划快照。

### 由谁产生

- 候选内容由规则规范化与候选计划模块产生；
- 合法性由计划校验模块确认；
- 用户确认由任务生命周期模块记录。

### 被谁使用

任务生命周期、HTTP 执行、确定性比较、证据和重复运行。

### 必须包含

- 验证目标；
- 任务类型；
- 计划版本标识；
- 绑定的 OpenAPI 快照；
- 一至三个有序请求步骤；
- 每一步的操作、输入和数据来源；
- 变量提取规则；
- 每一步预期；
- 最终判断条件；
- 前置条件；
- 测试环境和测试数据；
- 副作用说明；
- 证据缺失条件；
- 确定性校验结果；
- 用户确认记录。

### 暂不决定

- 比较运算符全集；
- JSON 字段路径语法；
- 序列化格式；
- 完整数据绑定表达式。

### 冻结原则

用户确认的是确定性校验通过后的完整计划快照，不是模型自由文本、自然语言摘要或候选草稿。

确认后只要步骤、环境、数据、预期、提取规则或判断条件发生变化，就必须形成新计划版本并重新确认。

---

## 5.5 验证尝试 `ValidationAttempt`

### 代表什么

代表针对一个验证任务进行的一次独立真实验证。

首次执行和每一次重复运行都产生新的验证尝试。

### 由谁产生

任务生命周期与范围控制模块。

### 被谁使用

HTTP 执行、确定性比较、证据报告和重复运行。

### 必须包含

- 所属验证任务；
- 尝试编号；
- 绑定的确认计划快照；
- 绑定的 OpenAPI 快照；
- 尝试生命周期状态；
- 创建、开始和结束时间；
- 本次执行意图标识；
- 执行记录引用；
- 评估结果引用；
- 四态业务结论；
- 是否为重复运行；
- 前一次尝试引用。

### 暂不决定

- 编号格式；
- 保留周期；
- 并发执行，因为 V0 不支持同一任务并发尝试。

---

## 5.6 步骤执行记录 `StepExecutionRecord`

### 代表什么

代表计划中一个逻辑步骤的真实 HTTP 执行事实。

### 由谁产生

HTTP 执行模块。

### 被谁使用

确定性比较、证据报告和重复运行对比。

### 必须包含

- 所属验证尝试；
- 对应计划步骤；
- 实际执行顺序；
- 每次实际发送编号；
- 实际 HTTP 方法、URL、参数、脱敏请求头和请求体；
- 请求时间；
- 响应时间或耗时；
- 实际状态码、响应头和响应体；
- 请求是否到达服务；
- 原始网络或执行异常；
- 提取变量及其来源；
- 后续步骤未执行的原因。

### 暂不决定

- 响应体大小上限的具体数值；
- TLS 和重定向策略细节；
- 原始报文保存格式。

---

## 5.7 确定性评估结果 `EvaluationResult`

### 代表什么

代表比较模块依据确认计划、OpenAPI 快照和真实执行记录产生的结构化判断。

四态结论是该对象的一部分，不建立独立结论对象。

### 由谁产生

确定性比较与结论模块。

### 被谁使用

任务生命周期、证据报告和重复运行对比。

### 必须包含

每个判断项：

- 判断项标识；
- 依据来源；
- 预期值或关系；
- 实际值；
- 比较结果；
- 证据引用；
- 无法比较原因。

整体结果：

- 必要步骤是否完成；
- 前置条件是否得到证明；
- 是否存在关键证据缺失；
- 是否存在环境或归因歧义；
- 四态业务结论；
- 固定判定依据。

### 暂不决定

- 具体比较器实现；
- 数值精度规则；
- 完整错误消息模板。

---

## 5.8 证据包 `EvidencePackage`

### 代表什么

代表一次验证尝试的机器可读、可追溯和不可覆盖的事实集合。

### 由谁产生

证据、报告与重复运行模块。

### 被谁使用

报告生成、重复运行比较、外部验收和人工审查。

### 必须包含或引用

- 原始任务输入快照；
- OpenAPI 上下文快照；
- 已确认计划快照；
- 用户确认记录；
- 验证尝试；
- 全部步骤执行记录；
- 原始异常；
- 确定性评估结果；
- 四态结论；
- 脱敏说明；
- 证据来源关系；
- 再次执行所需信息边界。

### 暂不决定

- 导出文件格式；
- 长期保存策略；
- 完整性摘要算法。

---

## 5.9 人类可读报告 `HumanReadableReport`

人类可读报告是派生对象，不是事实来源。

### 必须包含

- 验证目标；
- 任务类型；
- 已确认计划摘要；
- 四态结论；
- 每项预期与实际；
- 请求、响应和异常证据；
- 缺失证据或失败原因；
- 最小复现步骤；
- 证据边界；
- 重复运行信息。

报告可以重新生成，但必须引用同一证据包，不得修改事实、判断结果和四态结论。

---

## 5.10 不作为独立顶层对象的内容

以下内容作为上述对象的内部组成：

- 原始业务规则：属于验证任务原始输入；
- 请求步骤：属于验证计划；
- 预期条件：属于验证计划；
- 变量提取规则：属于计划步骤；
- 四态结论：属于确定性评估结果和验证尝试；
- 重复运行记录：表现为新的验证尝试；
- 多次运行比较：是基于多个证据包的派生结果。

---

# 6. 模块间数据流

一次任务的数据流为：

```text
用户原始输入
    ↓
验证任务
    ↓
OpenAPI 上下文快照
    ↓
规范化规则 + 候选计划
    ↓
确定性计划校验
    ↓
已校验计划版本
    ↓
用户确认精确计划快照
    ↓
任务进入 READY
    ↓
创建验证尝试
    ↓
最多三个步骤执行记录
    ↓
确定性评估结果
    ↓
证据包封存
    ↓
验证尝试进入 COMPLETED 并绑定四态结论
    ↓
人类可读报告
```

## 6.1 用户输入到规范化规则

任务生命周期模块保存用户原始输入，不允许模型覆盖原文。

OpenAPI 模块先形成相关操作上下文。规则规范化模块接收：

- 原始验证目标；
- 原始业务规则；
- 任务类型；
- 相关 OpenAPI 上下文；
- 非敏感测试数据说明；
- V0 固定限制。

模型输出规范化规则和候选计划，但这些输出不能直接执行。

## 6.2 OpenAPI 如何参与候选计划

OpenAPI 上下文用于：

- 限定模型只能在已知操作中提出候选接口；
- 提供参数、请求体和响应契约；
- 约束计划步骤的输入形式；
- 为后续契约比较保留确定性依据。

## 6.3 候选计划如何确定性校验

计划校验模块检查：

- 单一目标；
- 三请求上限；
- 顺序执行；
- 无分支、循环和并发；
- 操作存在；
- 数据来源明确；
- 变量引用合法；
- 判断条件可执行；
- 副作用和缺失证据明确。

校验失败时只能返回问题，不得静默改变业务含义。

## 6.4 用户确认的对象

用户确认完整的已校验计划快照，包括：

- 目标接口；
- 请求顺序；
- 测试环境；
- 测试数据；
- 前置条件；
- 业务预期；
- 变量提取规则；
- 判断条件；
- 副作用；
- 证据不足条件。

## 6.5 HTTP 执行器的输入

HTTP 执行器只接受：

1. 已确认计划快照；
2. 绑定的 OpenAPI 快照；
3. 当前验证尝试标识；
4. 运行时认证信息；
5. 已由前序步骤确定性产生的变量。

执行器不读取用户自由文本、模型自由文本或报告。

## 6.6 比较模块的依据

比较模块只依据：

- 已确认计划；
- OpenAPI 契约；
- 当前尝试的全部实际执行记录；
- 变量提取结果；
- 原始异常；
- 步骤完整性信息。

模型对结果的意见不属于比较输入。

## 6.7 报告如何引用证据

报告必须能够把每个结论定位到：

- 对应计划条件；
- 对应执行步骤；
- 对应请求、响应或异常；
- 对应确定性比较结果。

报告内容不得成为新的事实来源。

## 6.8 重复运行

重复运行复用：

- 同一个验证任务；
- 同一验证目标；
- 同一已确认计划快照；
- 同一 OpenAPI 快照；
- 同一请求步骤、顺序和判断条件。

重复运行重新产生：

- 新验证尝试；
- 新执行意图；
- 新请求和响应；
- 新异常；
- 新比较结果；
- 新四态结论；
- 新证据包；
- 新报告。

修改目标、接口、步骤、环境、测试数据语义、预期、提取规则、判断条件或 OpenAPI 依据时，不属于严格重复运行，必须形成新计划并重新确认。

---

# 7. 状态所有权与状态机

## 7.1 验证任务状态

验证任务拥有准备和可运行状态：

- `DRAFT`
- `PREPARING`
- `AWAITING_CONFIRMATION`
- `READY`
- `CANCELLED`

### 状态定义

#### `DRAFT`

原始输入已创建，仍可补充或修改。

#### `PREPARING`

正在进行输入检查、OpenAPI 处理、规则规范化、候选计划生成和确定性计划校验。

#### `AWAITING_CONFIRMATION`

已经存在一份确定性校验通过的计划，等待用户确认、修改或拒绝。

#### `READY`

已经存在一份未被修改的确认计划，可以创建首次或重复验证尝试。

任务在某次尝试执行期间仍保持 `READY`，但创建尝试门禁保证同一任务最多一个活动尝试。

#### `CANCELLED`

用户在真实执行前决定不再继续当前任务。取消不创建验证尝试，也不产生四态结论。

## 7.2 验证尝试状态

验证尝试拥有执行状态：

- `EXECUTING`
- `COMPLETED`

#### `EXECUTING`

验证尝试已经创建，真实执行、比较或证据封存尚未全部完成。此时四态结论为空。

#### `COMPLETED`

本次尝试已经终止，确定性评估结果和证据包已经固化，不再允许修改。

`COMPLETED` 不等于验证通过。

## 7.3 四态业务结论

只有 `COMPLETED` 的验证尝试才能拥有一个四态结论：

- `PASSED`
- `SUSPECTED_DEFECT`
- `INCONCLUSIVE`
- `EXECUTION_FAILED`

以下组合全部合法：

- `COMPLETED + PASSED`
- `COMPLETED + SUSPECTED_DEFECT`
- `COMPLETED + INCONCLUSIVE`
- `COMPLETED + EXECUTION_FAILED`

验证任务不保存权威单一最终结论。任务可以派生显示最近一次结论、尝试数量或是否曾出现疑似缺陷，但这些都不得覆盖历史尝试。

## 7.4 任务状态转换

```text
DRAFT
  ↓ 提交准备
PREPARING
  ├─ 需要补充或修改 → DRAFT
  ├─ 形成有效计划 → AWAITING_CONFIRMATION
  └─ 用户取消 → CANCELLED

AWAITING_CONFIRMATION
  ├─ 用户要求修改 → PREPARING
  ├─ 用户拒绝并放弃 → CANCELLED
  └─ 用户确认 → READY

READY
  ├─ 发起执行 → 创建新的 ValidationAttempt
  ├─ 发起重复运行 → 创建新的 ValidationAttempt
  └─ 修改计划 → PREPARING
```

## 7.5 验证尝试状态转换

```text
创建 ValidationAttempt
        ↓
    EXECUTING
        ↓
确定性比较和证据封存完成
        ↓
    COMPLETED
        +
四态结论之一
```

## 7.6 取消与执行中断

- 用户在首次真实请求发送前取消：任务可进入 `CANCELLED`，不产生尝试和四态结论；
- 验证尝试进入 `EXECUTING` 后发生中断：不得退回普通取消；
- 已发生的请求、响应和异常必须保存；
- 若必要执行未完成，默认形成 `COMPLETED + EXECUTION_FAILED`；
- 若全部必要事实已获得，则继续完成确定性比较和证据封存。

---

# 8. 确定性编排规则

## 8.1 编排层定位

编排层是薄型、确定性的状态协调器，只负责：

- 检查当前状态是否允许下一动作；
- 调用对应模块；
- 接收和传递稳定数据对象；
- 执行合法状态转换；
- 记录明确错误；
- 保证模型、执行器和比较器不越权。

编排层不负责理解业务规则或判断缺陷。

## 8.2 完整流程

```text
创建验证任务
→ 输入与范围检查
→ 获取并冻结 OpenAPI 上下文
→ 规则规范化
→ 生成候选计划
→ 确定性计划校验
→ 用户确认或拒绝
→ 任务进入 READY
→ 创建验证尝试
→ 顺序执行最多三个实际 HTTP 请求
→ 提取步骤变量
→ 确定性比较
→ 产生待固化评估结果
→ 封存证据包
→ 验证尝试进入 COMPLETED 并绑定四态结论
→ 派生人类可读报告
→ 支持基于同一计划重复运行
```

## 8.3 各阶段规则

### 创建任务

进入条件：用户提供至少能够形成草稿的输入。

成功输出：`DRAFT` 验证任务。

终止条件：创建失败时不产生任务和尝试。

### 输入与范围检查

进入条件：任务处于 `PREPARING`。

成功输出：通过检查的任务输入。

失败条件：输入缺失、多个目标、任务类型不支持、明显超过 V0 范围。

处理：返回 `DRAFT`，记录准备错误，不产生四态结论。

### 获取 OpenAPI

进入条件：输入通过且来源明确。

成功输出：冻结的 OpenAPI 上下文快照。

失败条件：获取失败、格式错误、解析失败、必要操作不存在。

处理：停止准备，允许修正来源或有限技术重试。

### 规则规范化和候选计划生成

进入条件：存在原始规则和 OpenAPI 快照。

成功输出：规范化规则和候选计划。

失败条件：模型调用失败、模型输出无法结构化、规则无法形成明确预期、必要信息缺失。

处理：不进入执行，不允许模型发明事实。

### 确定性计划校验

进入条件：存在候选计划、任务约束和 OpenAPI 快照。

成功输出：已校验计划版本，任务进入 `AWAITING_CONFIRMATION`。

失败条件：违反单目标、三请求、顺序执行、操作存在、变量来源或比较能力等规则。

处理：返回明确校验问题，重新准备。

### 用户确认

进入条件：任务处于 `AWAITING_CONFIRMATION`。

成功输出：绑定精确计划版本的确认记录，任务进入 `READY`。

其他处理：

- 用户要求修改：回到 `PREPARING`；
- 用户拒绝并放弃：进入 `CANCELLED`。

### 创建验证尝试

进入条件：

- 任务为 `READY`；
- 确认计划仍有效；
- 运行时认证信息满足计划要求；
- 同一任务没有活动尝试；
- 执行意图未被处理。

成功输出：新的 `EXECUTING` 验证尝试。

失败条件：未确认计划、计划版本失效、重复执行意图、已有活动尝试、缺少运行时认证。

处理：不创建新尝试。

### HTTP 执行和变量提取

进入条件：尝试为 `EXECUTING`，已绑定确认计划和 OpenAPI 快照。

成功输出：一至三个不可变步骤执行记录及变量值。

失败条件：网络、认证、超时、解析、变量提取、用户中止或内部异常。

处理：保存已有事实，停止必要后续步骤，进入确定性比较。

### 确定性比较

进入条件：不再发送请求，已有计划、快照和执行记录。

成功输出：确定性评估结果和待固化四态结论。

失败处理：

- 证据不足：根据规则产生 `INCONCLUSIVE`；
- 技术或内部错误导致有效验证无法完成：产生 `EXECUTION_FAILED`。

### 证据封存和完成

进入条件：评估结果已形成，已有执行事实固定。

成功输出：不可修改的证据包。

只有证据包封存成功后，验证尝试才能进入 `COMPLETED` 并绑定四态结论。

### 报告生成

进入条件：验证尝试已 `COMPLETED`。

成功输出：人类可读报告。

报告生成失败不得改变尝试状态、评估结果和证据包。

---

# 9. 七个模块的概念接口边界

## 9.1 任务生命周期与范围控制

### 接收

- 用户任务输入；
- 当前任务状态；
- OpenAPI 处理结果；
- 已校验计划；
- 用户确认、修改或取消决定；
- 执行和重复运行命令；
- 尝试完成结果引用。

### 输出

- 验证任务；
- 合法状态转换；
- 用户确认记录；
- 新验证尝试；
- 准备错误和门禁结果。

### 明确错误

- 输入不完整；
- 多目标或超范围；
- 非法状态转换；
- 计划版本过期；
- 未确认计划请求执行；
- 已有活动尝试；
- 重复执行意图；
- 任务已取消。

### 不允许读取

- 模型内部推理；
- 明文认证凭据；
- 报告文字作为状态依据。

### 不允许修改

- 已确认计划；
- OpenAPI 快照；
- 历史执行记录；
- 已封存证据；
- 历史四态结论。

---

## 9.2 OpenAPI 上下文

### 接收

- OpenAPI 来源；
- 允许的接口范围；
- 当前任务需要的上下文范围。

### 输出

- OpenAPI 上下文快照；
- 相关操作、参数、请求体和响应契约；
- 来源和解析诊断。

### 明确错误

- 来源不可访问；
- 文件不可读取；
- 格式不合法；
- 规范解析失败；
- 不支持的规范内容；
- 目标操作不存在或不唯一。

### 不允许读取

- 模型输出；
- 运行时凭据；
- 真实执行响应；
- 历史结论。

### 不允许修改

- 任务目标；
- 计划；
- 用户测试数据；
- 执行记录和结论。

---

## 9.3 规则规范化与候选计划

### 接收

- 原始验证目标；
- 原始业务规则；
- 任务类型；
- 筛选后的 OpenAPI 上下文；
- 非敏感测试数据说明；
- V0 固定限制。

### 输出

- 规范化规则；
- 缺失信息和歧义；
- 候选操作映射；
- 候选计划。

### 明确错误

- 模型服务不可用；
- 调用超时；
- 输出无法结构化；
- 输出缺少必要内容；
- 规则无法形成可观察预期；
- 接口映射仍有歧义。

### 不允许读取

- 明文认证信息；
- 真实执行响应；
- 历史比较结果；
- 其他任务证据。

### 不允许修改

- 用户原始输入；
- OpenAPI 快照；
- 已确认计划；
- 验证尝试；
- 四态结论。

---

## 9.4 计划校验

### 接收

- 候选计划；
- 规范化规则；
- 任务约束；
- OpenAPI 快照；
- 用户测试数据；
- V0 确定性比较能力。

### 输出

- 计划校验结果；
- 通过校验的计划版本；
- 逐项校验问题；
- 是否可进入用户确认。

### 明确错误

- 多目标；
- 超过三次请求；
- 包含循环、分支、并发或动态探索；
- 操作不存在；
- 数据来源不明；
- 引用未来变量；
- 判断条件不可执行；
- 使用不支持的比较方式；
- 副作用或证据缺失条件不明确。

### 不允许读取

- 真实执行记录；
- 历史结论；
- 明文凭据；
- 模型推理过程。

### 不允许修改

- 用户原始输入；
- OpenAPI 快照；
- 候选计划的业务含义。

---

## 9.5 HTTP 执行

### 接收

- 已确认计划；
- 绑定的 OpenAPI 快照；
- 验证尝试标识；
- 运行时认证信息；
- 已确定的前序变量。

### 输出

- 每次实际发送记录；
- 实际请求和响应；
- 时间和顺序；
- 原始异常；
- 技术重试记录；
- 可供提取的响应事实。

### 明确错误

- 认证失败；
- DNS、连接、TLS 和网络错误；
- 请求超时；
- 用户中止；
- 响应被截断；
- 响应解码失败；
- 请求构造错误；
- 进程异常中断。

### 不允许读取

- 用户自由文本；
- 模型自由文本；
- 报告；
- 其他尝试结果；
- 未确认计划；
- 当前快照之外的接口。

### 不允许修改

- 计划步骤；
- 请求顺序；
- 判断条件；
- OpenAPI 契约；
- 已完成执行记录；
- 四态结论。

---

## 9.6 确定性比较与结论

### 接收

- 已确认计划；
- OpenAPI 快照；
- 当前尝试全部执行记录；
- 变量提取结果；
- 步骤完整性和原始异常。

### 输出

- 逐项预期、实际和结果；
- 证据引用；
- 前置条件证明状态；
- 证据缺失状态；
- 执行完整性；
- 四态结论和固定判定依据。

### 明确错误

- 必要证据缺失；
- 前置条件无法证明；
- 响应无法作为比较输入；
- 计划与执行记录不一致；
- 不支持规则漏入执行阶段；
- 比较器内部错误。

### 不允许读取

- 用户自由文本；
- 模型意见；
- 报告措辞；
- 明文认证信息；
- 计划外业务知识。

### 不允许修改

- 计划预期；
- OpenAPI 快照；
- 实际请求响应；
- 原始异常。

---

## 9.7 证据、报告与重复运行

### 接收

- 任务和尝试引用；
- OpenAPI 快照；
- 已确认计划；
- 用户确认记录；
- 执行记录；
- 确定性评估结果；
- 历史证据包。

### 输出

- 已脱敏和封存的证据包；
- 人类可读报告；
- 多次运行派生对比；
- 基于原计划的重复运行请求。

### 明确错误

- 敏感信息无法安全处理；
- 证据引用不完整；
- 数据完整性不一致；
- 证据封存失败；
- 报告生成失败；
- 重复运行引用的计划失效；
- 修改计划被错误当作重复运行。

### 不允许读取

- 明文运行时认证信息；
- 未结构化模型输出；
- 与当前尝试无关的数据。

### 不允许修改

- 原始请求响应；
- 原始异常；
- 比较结果；
- 四态结论；
- 历史证据包。

---

# 10. 最小失败分类

## 10.1 验证尝试创建前的错误

以下问题发生在准备或确认阶段，不创建验证尝试，也不产生四态结论：

- 用户输入不完整或不合法；
- OpenAPI 获取失败；
- OpenAPI 解析失败；
- 大模型调用失败；
- 大模型输出无法结构化；
- 候选计划未通过确定性校验；
- 用户要求修改；
- 用户拒绝或执行前取消；
- 执行前缺少运行时认证信息；
- 执行前 ApiGuard 内部错误。

这些错误可以形成准备阶段审计记录。

## 10.2 可以修改输入后重新准备

- 缺少 OpenAPI、环境地址、验证目标或测试数据；
- 多目标输入；
- 任务类型或请求数量超范围；
- OpenAPI 地址或文件错误；
- 规则缺少明确预期；
- 目标接口存在歧义；
- 认证信息尚未提供。

## 10.3 必须修改计划并重新确认

以下变化需要新计划版本：

- 目标接口；
- 请求步骤或顺序；
- 测试环境；
- 主要测试数据；
- 前置条件；
- 业务预期；
- 判断条件；
- 变量提取规则；
- OpenAPI 快照；
- 请求副作用；
- 是否允许安全技术重试。

## 10.4 `INCONCLUSIVE`

`INCONCLUSIVE` 只用于：

- 已经执行至少一个真实请求；
- 已获得真实响应或其他有效执行事实；
- 当前证据不能证明通过；
- 当前证据也不能可靠证明疑似缺陷；
- 问题本质是证据不足，而不是单纯技术无法执行。

典型情况：

- 前置状态没有被证明；
- 测试数据真实状态不确定；
- 后置状态不可观察；
- 响应被截断导致关键证据缺失；
- 变量缺失导致后续必要证据无法获得；
- 存在无法排除的环境或并发干扰；
- 已有响应不足以支持归因。

## 10.5 `EXECUTION_FAILED`

验证尝试创建后，因技术、环境、中断或内部错误无法完成有效验证时，产生 `EXECUTION_FAILED`。

典型情况：

- 有效认证本应是业务验证前提，但认证失败；
- DNS、连接、TLS 或网络失败；
- 请求超时且允许的技术重试已耗尽；
- 请求未到达预期验证边界；
- 进程异常退出；
- 执行开始后用户中止；
- ApiGuard 内部执行或比较错误；
- 请求已发送但结果未知，且不能安全重试。

## 10.6 4xx 和 5xx 的处理

待测 API 返回 4xx 或 5xx 本身不能自动判定为 `EXECUTION_FAILED`。

4xx 或 5xx 是真实 HTTP 响应，可能是：

- 计划预期的业务拒绝；
- OpenAPI 已声明的错误响应；
- 与明确业务预期冲突的缺陷证据；
- 网关或认证层导致目标业务未执行；
- 证据不足的异常响应。

比较模块必须根据确认计划决定其意义。

## 10.7 执行开始后的用户中止

执行中止必须：

1. 保留已经发送的请求和获得的响应；
2. 记录中止时间、步骤和未执行步骤；
3. 不再发送尚未开始的后续请求；
4. 若所有必要事实已获得，则继续比较；
5. 若中止导致有效验证无法完成，形成 `COMPLETED + EXECUTION_FAILED`；
6. 不退回任务级 `CANCELLED`。

---

# 11. 重试与幂等边界

## 11.1 OpenAPI 获取重试

允许至多一次自动技术重试，仅针对暂时性网络问题。

不自动重试：

- 文档格式错误；
- 权限或地址明确错误；
- 不支持的规范；
- 目标操作不存在。

## 11.2 大模型调用重试

允许：

- 调用超时、限流或临时服务错误时至多一次自动重试；
- 结构化输出失败时至多一次格式修复调用。

格式修复只能修复结构和缺失字段，不得改变业务含义。

不允许：

- 自动反复重规划；
- 无限自我反思循环；
- 因计划校验失败而持续调用模型；
- 在执行期间重新调用模型调整计划。

## 11.3 待测 HTTP 请求重试

默认不自动重试。

### GET 和 HEAD

只有同时满足以下条件时，允许一次有限技术重试：

- 方法为 `GET` 或 `HEAD`；
- 没有获得任何 HTTP 响应；
- 失败属于暂时性网络问题；
- 确认计划明确允许重试；
- 重试后仍不超过三个实际请求上限。

### POST、PUT、PATCH、DELETE

默认禁止自动重试，因为第一次请求可能已经到达服务，重发可能造成重复创建、修改、删除、扣款或其他副作用。

V0 不实现通用幂等键管理、事务补偿或自动副作用回滚。

## 11.4 实际请求上限

一个验证尝试最多向待测服务发送三个实际 HTTP 请求。

技术重试也计入上限。

## 11.5 技术重试记录

技术重试属于同一个逻辑步骤，但每次实际发送都必须形成独立不可变事实，包括：

- 发送序号；
- 请求时间；
- 实际请求；
- 原始错误或响应；
- 允许重试的依据；
- 哪一次结果用于最终步骤结果。

不能只保存最后一次成功结果。

## 11.6 防止重复创建验证尝试

V0 采用最小幂等控制：

- 每次用户执行提交一个唯一执行意图标识；
- 同一任务、同一计划、同一执行意图只能创建一个验证尝试；
- 重复提交相同执行意图时返回已创建尝试；
- 同一任务存在 `EXECUTING` 尝试时拒绝创建另一个；
- 已完成后明确重复运行必须使用新的执行意图。

V0 不实现分布式锁、全局 exactly-once、消息队列去重或智能重复检测。

## 11.7 技术重试与重复运行

技术重试：

- 属于同一验证尝试；
- 处理暂时性传输故障；
- 不产生独立四态结论；
- 受同一尝试三个实际请求上限约束。

用户重复运行：

- 创建新的验证尝试；
- 重新执行完整确认计划；
- 产生新的四态结论和证据包；
- 通常要求用户确认测试环境状态已恢复。

---

# 12. 最多三步的变量传递

## 12.1 允许的变量来源

计划可以预先定义：

- 用户提供的固定值；
- 前一步响应状态码；
- 前一步明确命名的非敏感响应头；
- 前一步 JSON 响应体中的固定路径。

变量限制为简单标量：

- 字符串；
- 数字；
- 布尔值；
- 明确允许的 `null`。

V0 不支持复杂对象、数组变换、任意脚本和动态表达式。

## 12.2 后续步骤引用

后续步骤只能引用：

- 用户输入变量；
- 已完成前序步骤；
- 计划中预先声明的变量名。

禁止：

- 引用未来步骤；
- 引用未声明变量；
- 执行期间临时创建变量；
- 根据变量选择不同接口；
- 根据变量动态增加、删除或分支步骤。

## 12.3 状态码、响应头和 JSON

- 状态码可以提取为数值变量，但通常用于判断；
- 响应头只允许提取明确命名的非敏感头；
- JSON 响应体只允许固定路径提取简单值。

V0 不允许：

- 模型从响应中语义猜测变量；
- 任意脚本执行；
- 复杂集合过滤；
- 从自然语言错误消息中猜测资源标识；
- 动态登录、Cookie 会话、Token 刷新或认证链路编排。

## 12.4 变量提取失败

提取失败时必须：

1. 保存原始响应；
2. 保存计划提取规则；
3. 保存失败原因；
4. 不填入猜测值；
5. 停止依赖该变量的后续步骤；
6. 进入确定性比较。

默认结论：

- 已有真实响应但缺少必要证据：通常 `INCONCLUSIVE`；
- 缺失字段本身就是明确验证目标且证据充分：可以 `SUSPECTED_DEFECT`；
- 非法提取规则漏过计划校验：`EXECUTION_FAILED`。

## 12.5 执行期间禁止修改提取规则

模型和执行器都不得在执行期间：

- 更换字段路径；
- 尝试其他字段名称；
- 改用响应头或自由文本解析；
- 查询替代接口；
- 调整预期适应实际响应。

提取规则变化必须形成新计划并重新确认。

## 12.6 禁止动态增加 HTTP 请求

动态增加请求会破坏：

- 用户确认的接口范围；
- 副作用确认；
- 三请求上限；
- 单一验证目标；
- 计划和证据追溯；
- 重复运行一致性。

即使缺少证据，V0 也只能报告缺失，不得自行增加未确认请求。

---

# 13. 不可变证据边界

## 13.1 计划确认后冻结

以下内容一经用户确认，不可直接修改：

- 验证目标快照；
- 规范化规则版本；
- OpenAPI 上下文快照；
- 请求步骤和顺序；
- 测试环境；
- 测试数据；
- 数据来源；
- 变量提取规则；
- 前置条件；
- 预期条件；
- 判断条件；
- 副作用说明；
- 用户确认记录。

修改时必须形成新版本并重新确认。

## 13.2 执行事实冻结

每次实际发送后，以下内容不可覆盖：

- 实际发送顺序；
- HTTP 方法和 URL；
- 参数；
- 脱敏后的请求头；
- 请求体；
- 请求时间；
- 响应时间和耗时；
- 状态码；
- 响应头和响应体；
- 请求是否到达服务；
- 原始异常；
- 提取变量；
- 未执行步骤原因。

重新执行必须创建新验证尝试或新的实际发送记录，不能覆盖旧事实。

## 13.3 评估结果冻结

证据包封存后，以下内容不可修改：

- 比较使用的计划版本；
- 比较使用的 OpenAPI 快照；
- 每项预期和实际；
- 每项比较结果；
- 证据引用；
- 前置条件证明状态；
- 执行完整性；
- 缺失证据；
- 四态结论；
- 固定判定依据。

## 13.4 可以重新生成

可以重新生成：

- 报告措辞；
- 报告章节顺序；
- 摘要长短；
- 复现步骤表达；
- 展示格式；
- 多次运行对比摘要；
- 更严格但不改变事实的展示脱敏。

重新生成不得改变证据和结论。

## 13.5 不使用复杂事件溯源

V0 使用：

- 版本化快照；
- 追加式执行记录；
- 追加式评估记录；
- 追加式报告版本；
- 数据库约束；
- 受限写入接口；
- 不变量测试。

V0 不实现完整事件溯源、事件重放、CQRS 或投影系统。

---

# 14. 技术架构选择

## 14.1 确定性编排

采用：

> 普通 Python 应用服务 + 显式领域状态机

不采用 LangGraph 和其他工作流框架。

原因：

- 当前流程固定、有限和无动态分支；
- 已有明确的任务状态、验证尝试和数据库快照；
- LangGraph 会引入第二套 Graph State、Thread、Checkpoint 和恢复语义；
- 会增加领域状态与框架状态一致性问题；
- 不使用 Agent 框架不影响项目的 Agent 价值表达。

ApiGuard 的 Agent 价值来自：

> 自然语言规则  
> → 模型语义转换  
> → 结构化候选计划  
> → 确定性约束  
> → 人类确认  
> → 真实工具执行  
> → 事实判断  
> → 证据反馈

只有后续版本出现执行期动态分支、模型选择工具、长时间暂停、多次中间人工审批或复杂重规划时，才重新评估 LangGraph。

## 14.2 HTTP API 服务

推荐：

- FastAPI；
- Uvicorn。

原因：

- 与项目目标用户和求职方向一致；
- 适合 HTTP API、OpenAPI 和服务端模板共存；
- 便于搭建参考验收服务；
- 项目不需要 Django 级平台能力。

暂不引入：

- Django/DRF；
- GraphQL；
- WebSocket。

## 14.3 配置管理

推荐：`pydantic-settings`。

用于：

- 模型供应商和模型名称；
- 模型密钥；
- 数据库路径；
- 请求超时；
- 文档和响应大小限制；
- 日志等级；
- 开发和测试环境配置。

暂不引入配置中心、Vault 或云 Secret Manager。

## 14.4 领域数据和输入校验

推荐：Pydantic v2。

用途：

- API 输入校验；
- 模型结构化输出校验；
- 计划和评估数据边界；
- 配置校验。

领域逻辑不得退化为无约束动态字典传递。

## 14.5 数据持久化

推荐：SQLAlchemy 2.x ORM。

原因：

- 任务、计划版本、尝试和证据存在清晰关系；
- 便于约束状态所有权和不可覆盖写入；
- 可以使用同一领域结构支持未来数据库迁移。

暂不引入：

- 复杂 Repository 框架；
- CQRS；
- 多数据库抽象。

## 14.6 数据库迁移

推荐：Alembic。

所有持久化结构变化必须形成可审查迁移记录。

## 14.7 OpenAPI 获取和解析

推荐：

- HTTPX 获取远程文档；
- JSON 标准解析；
- PyYAML `safe_load` 解析 YAML；
- openapi-core 或等价的成熟 OpenAPI 3.0/3.1 校验能力。

V0 推荐支持：

- OpenAPI 3.0.x；
- OpenAPI 3.1.x。

V0 不支持：

- Swagger 2.0；
- OpenAPI 3.2；
- 自己实现完整规范解析器。

## 14.8 待测 HTTP 请求执行

推荐：HTTPX 同步 Client。

原因：

- V0 执行为同步、顺序和有限请求；
- 异常类型、超时和测试 Transport 清晰；
- 不需要异步并发。

V0 不使用浏览器自动化、curl 子进程或分布式执行器。

## 14.9 JSON Schema 和响应结构校验

推荐优先使用成熟 OpenAPI 验证能力完成契约验证，并在需要时使用 `jsonschema` 处理明确的 Schema 校验。

V0 只验证冻结范围内的：

- 状态码；
- 内容类型；
- JSON 结构；
- 必填字段；
- 基础类型。

不引入模糊测试、属性测试和完整安全扫描。

## 14.10 大模型结构化输出

推荐：

- 供应商官方 SDK；
- 内部供应商适配接口；
- 原生 JSON Schema Structured Output（供应商支持时）；
- 本地 Pydantic 二次校验。

V0 只实现一个实际供应商，但领域层不得依赖供应商响应类型。

暂不引入：

- LangChain；
- LiteLLM 多供应商路由；
- 多模型并行；
- 工具调用 Agent。

## 14.11 报告生成

推荐：Jinja2。

报告可以生成 HTML 和 Markdown 或文本内容。

V0 不引入 PDF 引擎、复杂报表平台和独立文档服务。

## 14.12 日志

推荐：Python 标准 `logging`。

日志至少包含可关联的：

- 任务标识；
- 验证尝试标识；
- 模块和阶段；
- 错误分类。

日志不能替代正式证据。

V0 不引入 ELK、分布式追踪平台和复杂可观测性基础设施。

## 14.13 测试

推荐：pytest。

辅助使用：

- FastAPI TestClient；
- HTTPX MockTransport；
- 参考 FastAPI 服务；
- 可重置测试数据 Fixture。

## 14.14 静态检查

推荐：

- Ruff：格式化和 lint；
- Pyright：静态类型检查。

不叠加 Black、isort、Flake8、mypy 等重复工具链。

## 14.15 依赖和项目管理

推荐：

- `pyproject.toml`；
- `uv`；
- `uv.lock`。

目标是保证本地、测试和容器安装一致。

## 14.16 容器化

推荐：

- Dockerfile；
- Docker Compose。

Compose 只包含：

- ApiGuard；
- FastAPI 参考验收服务。

SQLite 数据目录通过本地卷持久化。

---

# 15. 数据库选择

## 15.1 正式选择

V0 使用 SQLite。

通过 SQLAlchemy 和 Alembic 保留未来迁移 PostgreSQL 的可能，但 V0 不承诺同时兼容两种数据库。

## 15.2 选择理由

SQLite 适合当前条件：

- 单应用进程；
- 单 Uvicorn Worker；
- 单机本地开发和演示；
- 写入量低；
- 同一任务最多一个活动尝试；
- 请求和响应规模有限；
- 不要求多用户高并发。

建议启用：

- 外键约束；
- WAL 模式；
- 明确事务边界。

## 15.3 当前不选择 PostgreSQL

PostgreSQL 更适合：

- 多应用进程；
- 多用户并发；
- 独立 Worker；
- 长期远程部署；
- 复杂 JSON 查询和统计；
- 更高写入并发。

这些不是 V0 当前需求。

## 15.4 Docker Compose 数据库边界

V0 的 Compose 不包含独立数据库容器。

未来只有出现多进程、多用户、独立 Worker、远程长期部署或 SQLite 写竞争等真实问题后，才重新立项迁移 PostgreSQL。

---

# 16. 执行模型与异常收尾

## 16.1 正式执行模型

一次验证尝试在普通 API 请求内同步完成。

流程：

```text
创建并持久化 EXECUTING 尝试
→ 执行最多三个请求
→ 每次实际发送后立即保存事实
→ 确定性比较
→ 封存证据包
→ 将尝试置为 COMPLETED 并绑定四态结论
→ 返回结果
```

V0 不使用 FastAPI BackgroundTasks 执行核心验证，不使用独立 Worker 和任务队列。

## 16.2 用户等待

用户发起执行后保持当前连接等待结果。

合理性：

- 请求最多三个；
- 每一步有明确超时；
- 不执行长轮询；
- 不执行文件传输；
- 不执行高计算任务。

客户端连接断开不等同于用户主动中止。用户重新打开页面后可以从持久化状态查看当前尝试。

## 16.3 进程异常后的本地收尾

应用启动时检查所有 `EXECUTING` 尝试：

- 如果必要请求、响应和评估输入均已完整保存：重新运行确定性比较和证据封存，不重复 HTTP；
- 如果存在请求结果未知或记录不完整：记录进程中断异常，形成 `EXECUTION_FAILED`，封存已有证据并置为 `COMPLETED`；
- 不自动重发结果未知的请求；
- 不重新发送 POST、PUT、PATCH、DELETE；
- 不创建新尝试冒充原尝试。

这只是本地未完成尝试的有限确定性收尾，不是通用恢复系统。

## 16.4 活动尝试门禁

同一任务最多一个活动尝试，通过：

- 数据库事务内检查和创建；
- 数据库唯一性约束；
- 执行意图幂等标识。

V0 不需要分布式锁。

---

# 17. 大模型接入边界

## 17.1 封装位置

模型调用只存在于规则规范化与候选计划模块。

## 17.2 模型输入

只允许包含：

- 验证目标；
- 原始业务规则；
- 任务类型；
- 相关 OpenAPI 操作上下文；
- 非敏感测试数据说明；
- V0 固定限制；
- 输出结构 Schema；
- 一次结构修复时的本地校验错误。

不得包含：

- Token、API Key、Cookie；
- 完整历史证据库；
- 其他任务响应；
- 执行器或数据库访问能力；
- 待测 API 工具；
- 未筛选敏感请求头。

## 17.3 输出约束

模型输出必须符合预定义结构，并至少覆盖：

- 前置条件；
- 动作；
- 可观察预期；
- 缺失信息；
- 候选操作；
- 一至三个候选步骤；
- 数据来源；
- 判断条件；
- 证据缺失条件。

即使供应商提供原生结构化输出，ApiGuard 仍必须本地校验。

## 17.4 一次结构修复

首次结构化失败后允许一次修复调用。

修复输入只包含：

- 原始模型输出；
- 结构错误；
- 输出 Schema；
- 只修复格式、不改变业务含义的约束。

第二次仍失败则停止准备。

## 17.5 供应商可替换性

- 领域层使用供应商无关输入输出对象；
- 模型供应商、模型名称和密钥来自配置；
- 一个适配器负责调用官方 SDK；
- V0 只实现一个实际供应商；
- 不实现多供应商路由和故障切换。

## 17.6 Prompt 版本

每次模型调用必须记录 Prompt 版本标识，用于：

- 复现候选计划；
- 分析结果变化；
- 模型评测；
- Prompt 回归。

Prompt 具体内容保存在代码仓库，持久化记录引用稳定版本。

## 17.7 原始模型输出

需要保存：

- 供应商；
- 模型标识；
- Prompt 版本；
- 原始输出；
- 结构化结果；
- 校验错误；
- 是否发生修复；
- 调用耗时；
- 可用时的供应商请求标识和 Token 用量。

原始模型输出属于规划审计信息，不是最终事实证据，也不得进入确定性比较。

## 17.8 模型不得进入执行和比较

原因：

- 防止执行期间改变请求；
- 防止动态增加接口；
- 防止修改判断标准；
- 保证同一计划可重复验证；
- 保证四态结论来源明确；
- 保证项目可测试和可审查。

---

# 18. 证据存储与安全边界

## 18.1 数据库存储

以下内容直接保存在 SQLite：

- OpenAPI 原始快照；
- OpenAPI 规范化上下文；
- 已确认计划快照；
- 用户确认记录；
- 请求方法、URL、参数和脱敏头；
- 请求体；
- 响应状态码、脱敏头和响应体；
- 原始异常；
- 比较输入；
- 比较结果；
- 四态结论；
- 证据包引用；
- 报告文本；
- 原始模型输出和结构化结果。

V0 不需要对象存储。

## 18.2 大文本和截断

V0 必须设置：

- OpenAPI 文档大小上限；
- 请求体大小上限；
- 响应体大小上限；
- 模型输出大小上限。

超过上限时必须保存：

- 已获得内容；
- 原始长度；
- 截断标识；
- 截断对结论的影响。

不得把截断内容伪装成完整证据。

## 18.3 认证信息

静态 Token、API Key 和 Cookie 只在本次运行内存中使用，不持久化。

数据库只保存：

- 认证方式；
- 使用了哪些头；
- 固定脱敏占位符；
- 可选的不可逆摘要。

重复运行时用户重新提供有效凭据。

V0 不引入凭据加密存储、主密钥管理、权限系统和 Secret Manager。

## 18.4 请求头和响应头脱敏

持久化前必须按大小写不敏感规则脱敏，至少覆盖：

- `Authorization`；
- `Proxy-Authorization`；
- `Cookie`；
- `Set-Cookie`；
- `X-API-Key`；
- 用户声明的其他认证头。

运行时原始请求对象和持久化证据对象必须分离，防止误存秘密。

用户明确标记的敏感请求体和响应体字段也必须递归脱敏。

---

# 19. 前端和交互边界

## 19.1 正式选择

采用：

> FastAPI + Jinja2 服务端模板 + 少量原生 JavaScript

FastAPI Swagger 保留为开发调试工具，不作为主要产品入口。

V0 不采用独立 React 或 Next.js 前端。

## 19.2 最小交互

最小前端只支持：

1. 创建任务；
2. 查看规范化规则；
3. 查看候选和已校验计划；
4. 确认、要求修改或拒绝计划；
5. 发起执行；
6. 查看尝试状态；
7. 查看四态结论和完整证据；
8. 基于同一确认计划发起重复运行。

## 19.3 不属于 V0

- 复杂视觉设计；
- 前端状态管理框架；
- 多用户登录；
- 组织和权限；
- 实时 WebSocket；
- 拖拽流程编排；
- 数据看板。

---

# 20. 测试与模型评测边界

## 20.1 软件测试

软件测试证明确定性代码是否符合冻结规则。

首次 V0 必须包括：

### 领域规则单元测试

- 状态所有权；
- 合法和非法状态转换；
- 单活动尝试；
- `COMPLETED` 后不可修改；
- 任务不保存权威四态结论。

### OpenAPI 解析测试

- OpenAPI 3.0/3.1；
- 操作提取；
- 参数和请求体；
- 响应契约；
- 非法文档和缺失操作。

### 计划校验测试

- 单目标；
- 三请求上限；
- 无循环、分支和并发；
- 数据来源；
- 变量引用；
- 用户确认门禁；
- 不支持条件。

### HTTP 执行测试

- URL 和请求构造；
- 认证注入；
- 顺序执行；
- 超时和网络错误；
- 有限技术重试；
- 请求计数；
- 中断处理。

### 变量提取测试

- 状态码；
- 明确响应头；
- JSON 固定路径；
- 缺失变量；
- 后续步骤终止。

### 确定性比较测试

- 契约比较；
- 精确值；
- 字段关系；
- 前后状态；
- 四态判定边界；
- 4xx/5xx 不自动等于执行失败。

### 证据测试

- 脱敏；
- 快照冻结；
- 追加式记录；
- 报告不改变事实；
- 重复运行不覆盖历史。

### 数据持久化测试

- 任务、计划、尝试和证据关系；
- 执行意图幂等；
- 活动尝试约束；
- 启动收尾；
- 数据迁移。

### API 集成测试

覆盖：

- 创建；
- 准备；
- 确认；
- 执行；
- 查看；
- 重复运行。

### 真实端到端验收

必须使用可重置状态的参考 FastAPI/OpenAPI 服务完成上位产品基线规定的八个固定场景：

1. 契约正确：`PASSED`；
2. 契约错误：`SUSPECTED_DEFECT`；
3. 业务规则正确：`PASSED`；
4. 业务规则错误：`SUSPECTED_DEFECT`；
5. 状态流程正确：`PASSED`；
6. 状态流程错误：`SUSPECTED_DEFECT`；
7. 已执行但证据不足：`INCONCLUSIVE`；
8. 技术、认证或执行失败：`EXECUTION_FAILED`。

Mock 只能辅助模块测试，不能替代真实 HTTP 外部证据。

## 20.2 Agent/模型评测

模型评测证明规则规范化和候选计划质量。

首次 V0 必须建立版本化小型评测集，覆盖：

- 三类任务；
- 明确规则；
- 缺少必要信息；
- 接口映射歧义；
- 多目标输入；
- 超过三个请求要求；
- 不可观察预期；
- 禁止发明测试数据。

最小指标：

- 结构化输出成功率；
- 前置条件提取正确性；
- 动作和预期正确性；
- 操作映射正确性；
- 缺失信息识别率；
- 超范围计划产生率；
- 通过确定性计划校验比例；
- 关键字段多次运行一致性。

## 20.3 软件测试与模型评测分离

软件测试：

- 输入和预期确定；
- 默认进入持续测试；
- 使用固定模型输出测试后续确定性流程；
- 失败通常阻止合并。

模型评测：

- 调用真实模型；
- 结果允许概率变化；
- 用于模型和 Prompt 决策；
- 在里程碑和版本验收时独立运行。

## 20.4 暂不引入

- 大规模模型评测平台；
- 多模型横向评测；
- LLM-as-a-judge；
- LangSmith；
- 负载和性能测试平台；
- 混沌测试；
- 浏览器端到端自动化。

---

# 21. 架构不变量

任何实现都必须遵守以下规则：

1. 未经确定性校验的候选计划不得视为可执行计划。
2. 未经用户确认的计划不得创建验证尝试。
3. 用户确认必须绑定精确计划版本。
4. 计划变化后旧确认失效，必须重新确认。
5. 验证任务不拥有权威四态结论。
6. 四态结论只能属于验证尝试。
7. 只有 `COMPLETED` 的验证尝试才能拥有四态结论。
8. `COMPLETED` 表示评估结果和证据包已固化，不表示验证通过。
9. 一个验证尝试只能绑定一个确认计划快照。
10. 一个验证尝试只能绑定一个 OpenAPI 快照。
11. 绑定后的计划和 OpenAPI 快照不得替换。
12. 一个验证尝试最多向待测服务发送三个实际 HTTP 请求。
13. 技术重试计入三个实际请求上限。
14. 请求必须按确认计划顺序执行。
15. 不允许循环、条件分支、并发和执行期重规划。
16. 后续请求数据只能来自用户输入或前序响应中的预先声明提取。
17. 大模型不得直接调用待测 API。
18. 大模型不得创建验证尝试。
19. 大模型不得修改确认计划。
20. 大模型不得覆盖确定性比较结果。
21. HTTP 执行器不得读取用户自由文本或模型自由文本。
22. HTTP 执行器不得增加接口、请求或改变顺序。
23. 比较模块只能依据确认计划、OpenAPI 快照和当前尝试实际证据。
24. 4xx 或 5xx 不得自动等同于 `EXECUTION_FAILED`。
25. 有副作用请求不得被自动重试。
26. 每次技术重试的实际发送都必须保留独立事实。
27. 执行开始后的中断不得退回任务级 `CANCELLED`。
28. 请求结果未知时不得自动重发有副作用请求。
29. 实际请求、响应、时间、异常、顺序和变量不可覆盖。
30. 比较输入、比较结果和最终结论不可覆盖。
31. 证据包未封存前不得将尝试标记为 `COMPLETED`。
32. 报告只能引用证据包，不得增加事实、修改结果或改变结论。
33. 报告生成失败不得改变已完成尝试。
34. 每次重复运行必须创建新的验证尝试。
35. 重复运行不得复用旧执行记录、评估结果和证据包。
36. 后一次尝试不得修改、替换或否定前一次尝试。
37. 同一验证任务同一时刻最多一个 `EXECUTING` 尝试。
38. 测试环境状态恢复由用户或待测环境负责。
39. 进程异常恢复不得重新发送结果未知的有副作用请求。
40. 日志、模型文本和报告均不得替代真实请求响应和确定性比较证据。

---

# 22. 明确不采用的技术和能力

ApiGuard V0 不采用：

- LangGraph；
- LangChain；
- 多 Agent；
- RAG；
- 长期记忆；
- 自动反思和执行期重规划；
- Celery；
- Redis；
- RabbitMQ；
- Kafka；
- Temporal；
- 独立 Worker；
- FastAPI BackgroundTasks 执行核心验证；
- PostgreSQL；
- 对象存储；
- React；
- Next.js；
- 微服务；
- Kubernetes；
- Helm；
- 服务网格；
- 分布式锁；
- 全局 exactly-once；
- 完整事件溯源；
- CQRS；
- 通用流程引擎；
- 自动回滚和事务补偿；
- 自动凭据管理；
- 复杂多用户平台能力。

这些能力只有在 V0 完整验收后，出现明确的新需求并重新立项时，才允许重新评估。

---

# 23. V0 技术架构冻结结论

ApiGuard V0 正式冻结为：

1. **应用形态：** 单进程模块化单体；
2. **编排方式：** 普通 Python 应用服务加显式领域状态机；
3. **后端框架：** FastAPI + Uvicorn；
4. **配置管理：** `pydantic-settings`；
5. **数据模型与校验：** Pydantic v2；
6. **持久化：** SQLAlchemy 2.x；
7. **数据库迁移：** Alembic；
8. **数据库：** SQLite；
9. **OpenAPI 获取：** HTTPX；
10. **OpenAPI 解析：** JSON、PyYAML `safe_load` 和成熟 OpenAPI 3.0/3.1 校验能力；
11. **待测请求执行：** HTTPX 同步 Client；
12. **契约与 Schema 校验：** OpenAPI 验证能力，必要时使用 `jsonschema`；
13. **大模型接入：** 供应商官方 SDK、内部适配器、结构化输出和本地 Pydantic 校验；
14. **报告：** Jinja2；
15. **日志：** Python 标准 `logging`；
16. **测试：** pytest、FastAPI TestClient、HTTPX MockTransport 和真实参考服务；
17. **静态检查：** Ruff + Pyright；
18. **依赖管理：** `pyproject.toml` + `uv` + `uv.lock`；
19. **执行模型：** 普通 API 请求内同步执行完整验证尝试；
20. **前端：** FastAPI + Jinja2 服务端模板 + 少量原生 JavaScript；
21. **本地部署：** Dockerfile + Docker Compose；
22. **Compose 服务：** ApiGuard 和参考 FastAPI 服务，不包含数据库、队列或 Worker；
23. **证据存储：** SQLite 中的版本化快照、追加式执行记录和追加式评估记录；
24. **异常收尾：** 启动时对本地 `EXECUTING` 尝试进行有限、确定性收尾，不重发结果未知的有副作用请求。

---

# 24. 范围自检

## 24.1 是否引入超出需求的基础设施

没有。

运行依赖只包括：

- 一个 Python 应用；
- 一个 SQLite 文件；
- 外部模型 API；
- 待测服务；
- 参考验收服务。

## 24.2 是否为了体现 Agent 使用不必要框架

没有。

模型只承担必须由语义能力完成的规则规范化和候选计划生成。真实执行、比较、结论和证据全部由确定性程序完成。

## 24.3 是否符合当前基础

符合。

技术主线集中在：

- Python；
- FastAPI；
- Pydantic；
- SQLAlchemy；
- HTTP；
- OpenAPI；
- pytest；
- 少量服务端 HTML。

不要求同时掌握分布式系统、复杂前端和 Agent 框架。

## 24.4 是否可以分阶段实现

可以按以下能力顺序逐步实施：

```text
领域状态与持久化
→ OpenAPI 上下文
→ 确定性计划和校验
→ 模型规则规范化
→ 单请求执行
→ 确定性比较和四态
→ 三步变量流程
→ 证据报告
→ 最小前端
→ 重复运行和异常收尾
→ 真实端到端验收
```

每个阶段都必须有独立可测试的外部结果。

## 24.5 是否能够形成真实外部验收结果

能够。

架构保留：

- 真实 OpenAPI 快照；
- 用户确认计划；
- 真实 HTTP 请求响应；
- 确定性比较；
- 四态结论；
- 不可覆盖证据；
- 独立重复运行；
- 第三方可复现报告。

因此能够支撑产品基线要求的八个固定场景、连续重复运行和跨服务真实验证。

---

# 25. 基线变更规则

本文档冻结后，后续实现不得自行加入新技术或扩大 V0 范围。

以下变化必须重新进入架构评审：

- 引入 LangGraph、Worker、队列、PostgreSQL、React、微服务或 Kubernetes；
- 改变任务与验证尝试的状态所有权；
- 允许模型进入执行或确定性比较；
- 允许执行期动态增加请求或修改计划；
- 放宽三次实际 HTTP 请求上限；
- 改变证据不可覆盖规则；
- 改变四态判定边界；
- 引入自动回滚、分布式执行或复杂恢复；
- 将 V0 扩展为多用户测试管理平台。

代码、测试、迁移记录、真实 HTTP 运行结果和证据报告是后续评审的事实来源。聊天中的未冻结讨论不得覆盖本文档。

---

# 26. 最终结论

ApiGuard V0 的架构已经完成冻结。

本版本选择最小、清晰、可测试和可解释的结构：

> 模块化单体  
> + 显式确定性状态机  
> + 受限大模型语义转换  
> + 用户确认门禁  
> + 最多三个真实 HTTP 请求  
> + 确定性比较和四态判定  
> + 不可覆盖证据  
> + SQLite 本地持久化  
> + 同步执行和最小服务端前端

后续阶段只允许在本文档边界内拆分实现任务，不再继续新增架构技术。
